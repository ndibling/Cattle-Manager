<Window x:Class="CattleManager.App.Views.InputDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        ui:WindowHelper.UseModernWindowStyle="True"
        Title="Input" SizeToContent="Height" Width="360"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <StackPanel Margin="20">
        <TextBlock x:Name="MessageText" TextWrapping="Wrap" FontSize="14" Margin="0,0,0,12"/>
        <TextBox x:Name="InputBox" FontSize="14" Padding="8,6" Margin="0,0,0,16"
                 KeyDown="InputBox_KeyDown"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Padding="16,8" Margin="0,0,8,0" Click="Cancel_Click"/>
            <Button Content="OK" Padding="16,8" IsDefault="True" Click="OK_Click"
                    Style="{StaticResource PrimaryButton}"/>
        </StackPanel>
    </StackPanel>
</Window>
